## 1. 탐구 주제

### 미적분Ⅰ의 평균변화율을 활용한 좌우 할선의 수렴과 미분가능성 탐구

기준점에 왼쪽과 오른쪽에서 접근하는 두 점을 설정하고,
두 점과 기준점을 연결하여 만들어지는 할선의 기울기들을 서로 비교했다.

두 점이 기준점에 가까워질수록 두 할선의 기울기가 어떻게 변하는지
함수의 종류에 따라 관찰하고 비교했다.

특히 단순한 다항함수뿐만 아니라 절댓값 함수와 구간별로 식이 달라지는
함수를 함께 비교하여 좌우에서 접근하는 변화율이 항상 같은 방식으로
나타나는지 탐구했다.

---

# 2. 탐구 동기

미적분Ⅰ에서 평균변화율과 미분계수를 학습하면서
두 점 사이의 평균변화율을 한 점에 점점 가깝게 가져가면
한 점에서의 순간적인 변화율을 생각할 수 있다는 것을 배우게 됬다.

하지만 공식을 이용하여 미분계수를 계산하는 것만으로는
실제로 두 점이 가까워지는 과정에서 할선의 기울기가 어떻게
변화하는지 직접 확인하기 어려운 점이 있었다.

그래서 깃허브와 스트림릿을 활용하여 기준점 하나와
그 점에 왼쪽과 오른쪽에서 접근하는 두 점을 설정하고,
두 할선의 기울기를 직접 계산하고 시각화하는 프로그램을 만들었다.

그래서 평균변화율이 미분계수로 연결되는 과정을
수치와 그래프로 직접 관찰하고자 했다.

---

# 3. 탐구 문제

4가지 질문을 중심으로 탐구했다.

### 질문 1

**h를 0에 가깝게 만들면 왼쪽과 오른쪽 할선의 기울기는 항상 같은 값에 가까워질까?**

### 질문 2

**함수의 종류가 달라져도 이러한 현상이 나타날까?**

### 질문 3

**두 방향에서 접근한 할선의 기울기가 서로 다르게 나타나는 함수에서는 어떤 특징이 있을까?**

### 질문 4

**평균변화율을 반복적으로 관찰하는 과정을 통해 미분계수의 의미를 어떻게 이해할 수 있을까?**

---

# 4. 탐구 방법

기준점을

A = (a, f(a))

로 정했다.

그리고 기준점에서 일정한 거리 h만큼 떨어진 두 점을 설정했다.

왼쪽 점:

C = (a-h, f(a-h))

오른쪽 점:

B = (a+h, f(a+h))

각 점을 기준점과 연결하여 두 개의 할선을 만들었다.

## 왼쪽 할선의 기울기

m_L = [f(a)-f(a-h)] / h

## 오른쪽 할선의 기울기

m_R = [f(a+h)-f(a)] / h

이후 h의 값을 점점 작게 만들면서
두 기울기의 변화를 비교했다.

---

# 5. 실험할 함수 선정

이번 탐구에서는 함수의 형태에 따라 결과가 달라지는지 확인하기 위해
다음과 같은 여섯 종류의 함수를 선정했다.

1. 2차함수
   - f(x) = x²

2. 3차함수
   - f(x) = x³

3. 4차함수
   - f(x) = x⁴

4. 복잡한 다항함수
   - f(x) = x⁴ - 2x³ - x² + 2x + 1

5. 절댓값 함수
   - f(x) = |x|

6. 구간으로 나뉜 함수

   x < 0 : f(x) = x²

   x ≥ 0 : f(x) = 2x²

1,2,3,4 함수는 차수와 다항함수를 활용하여 복잡하게 하였으며
5,6 함수는 특정 지점에서 좌우 변화율이 어떻게
달라질 수 있는지 확인하기 위해 선정했다.

---

# 6. 1차 탐구 - 이차함수

첫 번째로 f(x)=x²을 선택하했다

기준점을 x=1로 설정하고 h의 값을 점점 작게 만들었다.

예를 들어 h=1일 때

왼쪽 할선의 기울기 = 1

오른쪽 할선의 기울기 = 3

===============================================================


h를 0.5로 줄이면

왼쪽 할선의 기울기 = 1.5

오른쪽 할선의 기울기 = 2.5

===============================================================


h를 0.1로 줄이면

왼쪽 할선의 기울기 = 1.9

오른쪽 할선의 기울기 = 2.1

===============================================================


h를 0.01로 줄이면

왼쪽 할선의 기울기 = 1.99

오른쪽 할선의 기울기 = 2.01



### 관찰

h가 작아질수록 두 할선의 기울기 차이가 줄어들었다.

왼쪽과 오른쪽의 기울기가 모두 2에 가까워지는 것을 확인할 수 있었다.

이를 통해 평균변화율을 이용하여 한 점에 가까이 접근하는 과정에서
두 할선의 기울기가 하나의 값에 가까워지는 모습을 직접 확인했다.

---

# 7. 2차 탐구 - 삼차함수

두 번째로 f(x)=x³을 선택하였다.

기준점을 x=1로 설정하여 h를 감소시키면서
좌우 할선의 기울기를 비교하였다.

h가 작아질수록 왼쪽과 오른쪽 할선의 기울기가
서로 가까워지는 현상을 관찰하였다.

또한 기준점을 x=0, x=-1 등으로 변경하여
같은 현상이 다른 위치에서도 나타나는지 확인하였다.

### 관찰

삼차함수에서는 기준점의 위치에 따라
좌우 할선이 가까워지는 값이 달라졌지만,
한 점에 가까워질수록 좌우 기울기가 서로 가까워지는
패턴을 확인할 수 있었다.

따라서 특정한 한 점에서만 나타나는 현상인지 확인하기 위해
기준점의 위치를 변화시키는 것이 중요하다고 판단하였다.

---

# 8. 3차 탐구 - 사차함수

세 번째로 f(x)=x⁴을 선택하였다.

이차함수와 삼차함수보다 그래프의 형태가 복잡해졌지만
같은 방법으로 기준점과 좌우의 접근점을 설정하였다.

h를 점점 작게 하면서 좌우 할선의 기울기를 비교하였다.

### 관찰

함수의 차수가 높아졌음에도
기준점에 충분히 가까워질수록 좌우 할선의 기울기가
서로 가까워지는 모습을 관찰할 수 있었다.

이를 통해 함수의 차수가 높아진다고 해서
평균변화율을 이용한 탐구 방법 자체가 달라지는 것은 아니라는 점을 확인하였다.

---

# 9. 4차 탐구 - 복잡한 다항함수

다음으로

f(x)=x⁴-2x³-x²+2x+1

을 사용하였다.

단순한 거듭제곱 함수와 비교하여
식이 복잡해졌을 때에도 같은 방식으로
좌우 할선의 변화를 관찰할 수 있는지 확인하였다.

기준점을 여러 위치로 이동시키면서
h를 감소시켰다.

### 관찰

함수의 식이 복잡해졌지만
프로그램에서는 동일하게 기준점과 접근점을 설정하고
평균변화율을 계산할 수 있었다.

이를 통해 함수의 식이 복잡해지더라도
'기준점을 정하고 h를 줄이면서 좌우 평균변화율을 비교한다'
는 탐구 방법을 그대로 적용할 수 있다는 것을 확인하였다.

---

# 10. 5차 탐구 - 절댓값 함수

다음으로 f(x)=|x|를 선택하였다.

이 함수에서는 특히 x=0을 기준점으로 설정하였다.

h의 값을 작게 만들면서 좌우 할선의 기울기를 비교하였다.

### 관찰

x=0의 왼쪽에서 접근하는 할선의 기울기는 -1에 가까워지고,

오른쪽에서 접근하는 할선의 기울기는 1에 가까워졌다.

중요한 점은 h를 계속 작게 만들어도
두 값이 하나의 같은 값으로 가까워지지 않았다는 것이다.

### 탐구에서 발견한 점

앞에서 실험한 다항함수와 달리
특정한 점에서 좌우의 변화율이 서로 다르게 나타날 수 있다는 것을
확인하였다.

따라서 'h를 작게 만든다'는 것만으로 모든 함수에서
좌우 할선의 기울기가 같은 값으로 모이는 것은 아니라는 점을
확인할 수 있었다.

---

# 11. 6차 탐구 - 구간으로 나뉜 함수

마지막으로 다음과 같은 함수를 사용하였다.

x < 0 : f(x)=x²

x ≥ 0 : f(x)=2x²

이 함수에서는 두 식이 바뀌는 경계점인 x=0을
중요한 탐구 지점으로 설정하였다.

왼쪽에서 접근할 때는 x²을 사용하고,
오른쪽에서 접근할 때는 2x²을 사용하여
좌우 할선의 기울기를 비교하였다.

### 관찰

하나의 함수 안에서도 구간에 따라 사용하는 식이 다르면
특정한 경계점에서 왼쪽과 오른쪽의 변화율이
서로 다르게 나타날 수 있다는 것을 확인하였다.

이를 통해 단순히 함수 전체의 모양만 보는 것이 아니라
함수의 식이 바뀌는 특정 지점에서
좌우 변화율을 따로 비교할 필요가 있다는 점을 알게 되었다.

---

# 12. 함수별 비교

| 함수 | 탐구 지점 | 관찰 내용 |
|---|---|---|
| x² | x=1 등 | h가 작아질수록 좌우 기울기가 하나의 값에 가까워짐 |
| x³ | 여러 기준점 | 기준점에 따라 수렴하는 값은 달라지지만 좌우 기울기가 가까워지는 현상을 관찰 |
| x⁴ | 여러 기준점 | 함수의 차수가 높아져도 좌우 할선의 변화를 관찰할 수 있음 |
| 복잡한 다항함수 | 여러 기준점 | 식이 복잡해져도 같은 평균변화율 비교 방법을 적용할 수 있음 |
| \|x\| | x=0 | 왼쪽과 오른쪽의 기울기가 서로 다른 값에 가까워짐 |
| 구간함수 | x=0 | 식이 바뀌는 경계에서 좌우 변화율의 차이를 관찰 |

---

# 13. 탐구를 통해 알게 된 점

처음에는 미분계수를 공식으로 계산하는 것에 익숙했지만,
이번 탐구를 통해 미분계수가 단순히 계산 결과가 아니라
**한 점에 가까워지는 평균변화율의 변화를 통해 이해할 수 있는 개념**이라는 점을 확인하였다.

특히 하나의 함수만 사용하는 것이 아니라
이차, 삼차, 사차, 복잡한 다항함수, 절댓값 함수,
구간함수를 비교하면서 함수의 형태와 특정 지점에 따라
좌우 할선의 기울기가 다르게 나타날 수 있다는 것을 알게 되었다.

또한 그래프와 수치 데이터를 동시에 확인하니
h가 작아지는 과정에서 두 할선이 어떻게 변화하는지를
수식만 볼 때보다 직관적으로 확인할 수 있었다.

---

# 14. 정보 기술을 활용한 탐구의 의미

Python과 Streamlit을 이용하여
기준점과 h의 값을 직접 변경할 수 있도록 프로그램을 제작하였다.

또한 Plotly를 활용하여 함수의 그래프와
좌우 할선을 동시에 표시하였다.

h의 값에 따른 좌우 할선의 기울기를 표로 정리하고
그래프로 나타내어 변화 과정을 비교할 수 있도록 하였다.

이를 통해 수학적 개념을 단순히 계산하는 데 그치지 않고
정보 기술을 활용하여 수학적 현상을 실험하고 시각화할 수 있었다.

---

# 15. 수학교육과와의 연계

이번 탐구는 수학교육과 진로와도 연결할 수 있다.

미분계수는 학생들이 공식 자체를 암기하는 것보다
평균변화율이 한 점에서의 변화율로 연결되는 과정을 이해하는 것이 중요하다.

이번 프로그램에서는 h의 값을 직접 변화시키고,
좌우 할선이 움직이는 모습을 그래프로 확인할 수 있도록 하였다.

따라서 이러한 시각화가 미분계수의 개념을 학습할 때
학생들의 직관적인 이해를 돕는 방법으로 활용될 수 있는지
생각해 볼 수 있었다.

특히 정상적으로 좌우 기울기가 가까워지는 함수와
그렇지 않은 함수를 함께 비교함으로써
단순한 공식 암기보다 함수의 변화 과정을 관찰하는 방식으로
미분가능성의 개념을 이해할 수 있다는 점에 의미를 두었다.

---

# 16. 최종적으로 확인하고자 한 것

이번 탐구에서 가장 중요하게 생각한 것은
'어떤 함수가 된다, 안 된다'를 단순하게 분류하는 것이 아니었다.

함수와 기준점을 바꾸면서

**h 감소**

→ **왼쪽 할선 기울기 변화**

→ **오른쪽 할선 기울기 변화**

→ **두 값 사이의 차이 변화**

→ **두 방향의 변화율이 어떻게 나타나는지 분석**

하는 과정을 반복하였다.

이를 통해 평균변화율과 미분계수의 관계를
수치와 그래프를 통해 직접 확인하고,
함수의 형태와 특정 지점에 따라 변화율의 양상이
어떻게 달라지는지 탐구하였다.

---

# 17. 앞으로의 발전 방향

이번 프로그램에서는 제한된 수의 함수를 대상으로 실험하였다.

추후에는 사용자가 직접 함수를 입력하거나
더 다양한 함수의 형태를 실험할 수 있도록 프로그램을 확장할 수 있다.

또한 여러 기준점에서 얻은 결과를 자동으로 저장하고 비교하여
함수의 종류와 기준점에 따른 좌우 할선의 변화 양상을
표와 그래프로 한 번에 비교하는 기능도 추가할 수 있다.

이를 통해 수학적 개념을 정보 기술로 실험하고 분석하는
탐구 프로그램으로 발전시킬 수 있을 것이다.
